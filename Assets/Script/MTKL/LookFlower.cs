using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class LookFlower : MonoBehaviour
{
    /*
    Vector2 playerpos;
    Vector2 flowerpos;
    int radius_light = 100;
    private void Awake()
    {
        playerpos = GetComponent<Player>().transform.position;
        flowerpos = GetComponent<Transform>().position;
    }
    // Start is called before the first frame update

    private void FixedUpdate()
    {
        float distpos_x = playerpos.x - flowerpos.x;
        float distpos_y = playerpos.y - flowerpos.y;
        float distpos = distpos_x * distpos_x + distpos_y * distpos_y;
        Debug.Log(distpos);
        if(distpos > radius_light)
        {
            this.gameObject.SetActive(false);
        }
        else
        {
            this.gameObject.SetActive(true);
        }
    }
    */
   
}


